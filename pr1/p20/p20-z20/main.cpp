#include <iostream>
#include <cmath>
using namespace std;

int main()
/* a {
    float x,y;
    cin >> x >> y;

    if ( (x*x+y*y>=4) and (x<=2) and (y<=x) and (y>=0) )
        cout << "yes";
    else
        cout << "no";

} */

/* b {
    float x,y;
    cin >> x >> y;

    if ( (sin(x)<=y) and (y<=0.5) and (y>=0) and (x>=0) and (x<=3.14) )
        cout << "yes";
    else
        cout << "no";
}*/

/* v {
    float x,y;
    cin >> x >> y;

    if ( ((2-x*x<=0) and (y>=x)) or ((2-x*x<=0) and (y<=x) and (y>=0)) )
        cout << "yes";
    else
        cout << "no";
}*/

/* g {
    float x,y;
    cin >> x >> y;

    if ( ((x*x-2<=y) and (y<=x)) or ((x*x-2<=y) and (y<=-x)) )
        cout << "yes";
    else
        cout << "no";
}*/

/* d {
    float x,y;
    cin >> x >> y;

    if ( (x*x+y*y<=1 and y>x) or (x*x+y*y<=1 and y<x and x<=0) )
        cout << "yes";
    else
        cout << "no";
}*/

/* e {
    float x,y;
    cin >> x >> y;

    if ( (x*x+y*y<=1 and y>=x and y>=-x) or (x*x+y*y<=1 and y<=x) )
        cout << "yes";
    else
        cout << "no";
}*/

/* j {
    float x,y;
    cin >> x >> y;

    if ( (1-x>=y and 2*x*x<=y) or (2*x*x<=y and y>=1-x and x<=1)  )
        cout << "yes";
    else
        cout << "no";
}*/
